-- mason window is too narrow
-- I can't even see the full version message of each language servers
-- 0.9 is still too small for long name such as "typescript-language-server"
-- Finally!!! 0.95 is decent enought
lvim.builtin.mason.ui.width = 0.8
